extends settings/layout

prepend html
	- const lang = "en";

prepend head
	- const pageTitle = "Emergency Wet Carpet Drying in Sydney | Book Now — drySafe"
	- const themeColor = "#ffffff"
	- const pageDescription = "Emergency wet carpet drying across Sydney. Book online in about 60 seconds and lock in your arrival time instantly. Fixed package prices from $302 per room, confirmed the moment you book."
	- const pageKeywords = ""
	- const pageUrl = "wet-carpet-drying.html";
	- const pageImage = "img/logo.svg";
	- const twitterImage = "img/logo.svg";
append head
append content
	include mixins/btn

	//- [A1] Wet-carpet campaign variant. Same design system + section stack as the
	//- main page; only the hero copy is rewritten for emergency carpet-drying intent,
	//- with "Sydney" native to the headline. promo.pug reads these overrides.
	- const heroTitle = "Wet Carpet Emergency in Sydney? Book Now — Arrival Time Locked In, in Seconds"
	- const heroSubhead = "Fast emergency carpet drying across greater Sydney. Fixed package prices, dispatch confirmed the moment you book."

	.site-header
		include components/header
	main
		include components/promo
		include components/reviews
		include components/painpoint
		include components/experience
		include components/how
		include components/process
		include components/history
		include components/community
		include components/why
		include components/response


		include components/protect


		include components/faq
		include components/icons

	include components/footer

append scripts
